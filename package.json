{
  "name": "numerologia-root",
  "version": "1.0.0",
  "description": "Root configuration for Render deployment",
  "scripts": {
    "postinstall": "cd numFront && npm install && npm run build && cd ../mongo && npm install",
    "start": "cd mongo && node app.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
